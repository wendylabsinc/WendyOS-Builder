# WirePlumber runs as the "pipewire" system user, which has no home directory,
# so its state store defaults to an unwritable /.local/state. Point it at
# /var/lib/wireplumber instead, and let systemd create it.

[Service]
StateDirectory=wireplumber
Environment=XDG_STATE_HOME=/var/lib
