# monaco's usb_2_hsphy takes vdda-pll-supply = <&refgen>; as a module nothing
# installs it, so the PHY defers forever and a400000.usb never probes. lemans'
# PHYs take vreg_l7a and need none of this.
CONFIG_REGULATOR_QCOM_REFGEN=y
