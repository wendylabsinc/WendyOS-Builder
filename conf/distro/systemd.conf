
# enable systemd (instead of sysvinit)
# (use systemd as PID 1)
DISTRO_FEATURES:append = " systemd usrmerge"
DISTRO_FEATURES:remove = " sysvinit"

# Prevent the old sysvinit feature from being backfilled into
# DISTRO_FEATURES. Wrynose oe-core renamed the variable
# (DISTRO_FEATURES_BACKFILL_CONSIDERED → DISTRO_FEATURES_OPTED_OUT) and
# rejects the old name via BB_RENAMED_VARIABLES, so only the new name is set.
DISTRO_FEATURES_OPTED_OUT += " sysvinit"

VIRTUAL-RUNTIME_init_manager = "systemd"
INIT_MANAGER = "systemd"

# provide compatibility for packages that still ship SysV scripts
# (optional)
VIRTUAL-RUNTIME_initscripts = "systemd-compat-units"
# VIRTUAL-RUNTIME_syslog = ""
# VIRTUAL-RUNTIME_base-utils-syslog = ""

# using '/home/root' as root user's home directory is not fully supported by
# systemd as the code hardcodes '/root'
ROOT_HOME = "/root"
