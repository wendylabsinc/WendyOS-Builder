# modules-load.d payload: force-load v4l2loopback at boot so the control
# device (/dev/v4l2loopback) exists before the WendyAgent device agent
# starts creating per-camera loopback nodes via ioctl.
v4l2loopback
