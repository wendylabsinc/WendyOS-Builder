# WendyOS owns the offline-start bound rather than relying on NetworkManager's
# vendor default. nm-online normally exits at 60 seconds; the service timeout is
# a slightly larger hard backstop in case that userspace timeout regresses.
[Service]
Environment=NM_ONLINE_TIMEOUT=60
TimeoutStartSec=70
