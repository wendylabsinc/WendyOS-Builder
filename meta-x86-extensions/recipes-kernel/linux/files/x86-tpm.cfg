# TPM 2.0 support for the x86 /data encryption stack (Phase 1,
# docs/plans/x86-security.md). Built in (=y) rather than modules: the x86 image
# has no initramfs, so building these in removes any module-load-ordering race
# against the /data LUKS unlock and lets the TPM RNG feed early kernel entropy.
# Covers both the Intel PTT firmware TPM (CRB interface) and discrete TPMs
# (TIS LPC/MMIO, and SPI).

CONFIG_TCG_TPM=y
CONFIG_TCG_TIS_CORE=y
CONFIG_TCG_TIS=y
CONFIG_TCG_CRB=y
CONFIG_TCG_TIS_SPI=y

# TCG_TIS_SPI is "depends on SPI" (drivers/char/tpm/Kconfig) and nothing in the
# genericx86-64 config chain enables the SPI core -- checked the whole
# common-pc-64 scc/cfg closure at the pinned SRCREV_meta, and the only "select
# SPI" in the kernel is arch/arm/mach-pxa. Without this line the SPI TPM driver
# is silently dropped whenever this fragment is applied on its own, so a board
# with a discrete SPI TPM would come up with no TPM at all. It only appeared to
# work because can.cfg happens to pin CONFIG_SPI=y and WENDYOS_CAN defaults to
# "1" on x86; turning CAN off exposed it. Declared here so this fragment stands
# alone. Duplicating the symbol is free: merge_config.sh only warns when two
# fragments give different values, and oe-core does not pass -w.
CONFIG_SPI=y

# TPM RNG -> kernel entropy and /dev/hwrng. HW_RANDOM must be =y: a =m disables
# HW_RANDOM_TPM when TCG_TPM is built in.
CONFIG_HW_RANDOM=y
CONFIG_HW_RANDOM_TPM=y

# HMAC + AES-CFB parameter encryption on the TPM command bus (interposer /
# bus-sniffing defense). Kernel 6.18 flipped this default off, so force it on.
# Matters most for a bus-attached discrete TPM.
CONFIG_TCG_TPM2_HMAC=y

# securityfs (/sys/kernel/security) exposes the firmware TPM event log at
# tpm0/binary_bios_measurements (needs CONFIG_EFI, already enabled in the BSP).
CONFIG_SECURITYFS=y
